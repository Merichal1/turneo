import 'package:flutter/material.dart';

class AdminPaymentsHistoryScreen extends StatelessWidget {
  const AdminPaymentsHistoryScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Historial de pagos (WIP)'));
  }
}
